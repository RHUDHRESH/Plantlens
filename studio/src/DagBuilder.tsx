/** Visual DAG builder over graph.json (Domain R). React Flow v12 API:
 * import { ReactFlow } from '@xyflow/react'; measured dims in node.measured.
 */
export function DagBuilder() {
  return null;
}
