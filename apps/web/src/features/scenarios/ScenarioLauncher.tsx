/**
 * ScenarioLauncher — list scenarios and start/stop them (the demo control). Calls
 * api/client.startScenario(id) / stopScenario(); the backend streams the result over the WS.
 *
 * WHAT TO BUILD
 *   - useQuery(getScenarios) → list from /api/v1/simulator/scenarios
 *   - a "Run" button per scenario (hero: "Run Motor Overload" → scn_motor_overload)
 *   - a status badge from the runtime store's scenario.state
 *   - a stop/reset button
 * CHUNK: 2 (UI), wired to the live map in Chunk 5
 */
// TODO(you): implement ScenarioLauncher.
export {};
